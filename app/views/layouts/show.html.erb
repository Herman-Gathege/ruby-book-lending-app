<h1><%= @book.title %></h1>
<p>Author: <%= @book.author %></p>
<p>ISBN: <%= @book.isbn %></p>
<p>Status: <%= @book.available ? "Available" : "Borrowed" %></p>
<%= link_to "Borrow", borrow_book_path(@book), method: :post if @book.available %>
